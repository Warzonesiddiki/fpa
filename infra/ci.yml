# NOTE — the live pipeline now lives at `.github/workflows/ci.yml` (WS-01).
# This file previously held a copy that never ran because `.github/` was git-ignored.
# Do NOT restore a copy here; edit `.github/workflows/ci.yml` and this pointer
# stays as the historical reference called out by docs/CI-CD.md.
# Differences vs the old copy (intentional): js-gates now runs schema-equality-check
# and docs-link-check --strict; unit-ts now runs both coverage gates — matching
# the local `npm run check` contract and CI-CD.md §2 stages 5/6.
